# Dependencies
node_modules/

# Build output
BE_TKB/dist/
BE_TKB/dist_patch/
FE_TKB/.next/

# Docs build output (local-only: large binaries + contains tokens)
docx_build/

# Python cache (docx_build tooling)
__pycache__/
*.pyc

# Token / secret scratch files
*.env
!.env.example
realtokens.env
tokens.env

# Environment
.env
.env.local
.env.production

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# IDE
.vscode/
.idea/
.claude/

# Scratch / Temp files
*.sql
# ...but Prisma migrations are source, not scratch. Without this the *.sql rule above
# silently drops every new migration and the next deploy runs against an old schema.
!BE_TKB/prisma/migrations/**/*.sql
*.tmp
*.bak
.qa_*

# tasteskill local skill files
.agents/
skills-lock.json
